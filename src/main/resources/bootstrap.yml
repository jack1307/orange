server:
  port: 9527

spring:
  profiles:
    active: dev
  application:
    name: orange
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-setension: yaml
        name: orange
      discovery:
        server-addr: 127.0.0.1:8848